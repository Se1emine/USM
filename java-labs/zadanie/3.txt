Лабораторная работа №3. Создание и использование исключений (Exceptions)
В программе требуется:

1.     Создать собственное исключение (class).

2.     Создать метод (throw), который может возбуждать это исключение(throws).

3.     Написать метод, перехватывающий и обрабатывающий исключение (try-catch), возбуждаемое другим методом.

 

Варианты заданий.

 

1.     Лабораторная работа №2. Создание и обработка собственных исключений.

В программе требуется:

·        Создать собственное исключение (class).

·        Создать метод (throw), который может возбуждать это исключение(throws).

·        Написать метод, перехватывающий и обрабатывающий исключение (try-catch), возбуждаемое другим методом.

Исключение: с консоли вводятся 10 чисел и записываются в массив. Добиться ввода только простых чисел.

 

2.     Лабораторная работа №2. Создание и обработка собственных исключений.

В программе требуется:

·        Создать собственное исключение (class).

·        Создать метод (throw), который может возбуждать это исключение(throws).

·        Написать метод, перехватывающий и обрабатывающий исключение (try-catch), возбуждаемое другим методом.

Исключение: с консоли вводится имя. Добиться ввода только тех имен, которых нет в массиве имен в программе. т.е. добавлять в массив только новые имена.

 

3.     Лабораторная работа №2. Создание и обработка собственных исключений.

В программе требуется:

·        Создать собственное исключение (class).

·        Создать метод (throw), который может возбуждать это исключение(throws).

·        Написать метод, перехватывающий и обрабатывающий исключение (try-catch), возбуждаемое другим методом.

Исключение: с консоли вводятся числа и записываются в массив размером 10. Добиться заполнения массива только четными числами.

 

4.     Лабораторная работа №2. Создание и обработка собственных исключений.

В программе требуется:

·        Создать собственное исключение (class).

·        Создать метод (throw), который может возбуждать это исключение(throws).

·        Написать метод, перехватывающий и обрабатывающий исключение (try-catch), возбуждаемое другим методом.

Исключение: с консоли вводится наименование товара и его цена, данные записываются в массивы. В программе имеется информация о товаре и о диапазоне цен на данный товар. Добиться ввода только цен в диапазоне, допустимом для соответствующего товара.

 

5.     Лабораторная работа №2. Создание и обработка собственных исключений.

В программе требуется:

·        Создать собственное исключение (class).

·        Создать метод (throw), который может возбуждать это исключение(throws).

·        Написать метод, перехватывающий и обрабатывающий исключение (try-catch), возбуждаемое другим методом.

Исключение: с консоли вводится дата (число.месяц.год). Добиться ввода только правильных дат.  Создать массив из 5-ти правильных дат.

 

6.     Лабораторная работа №2. Создание и обработка собственных исключений.

В программе требуется:

·        Создать собственное исключение (class).

·        Создать метод (throw), который может возбуждать это исключение(throws).

·        Написать метод, перехватывающий и обрабатывающий исключение (try-catch), возбуждаемое другим методом.

Исключение: с консоли вводится время суток (чч:мм:сс). Добиться ввода только правильного времени.

 

7.     Лабораторная работа №2. Создание и обработка собственных исключений.

В программе требуется:

·        Создать собственное исключение (class).

·        Создать метод (throw), который может возбуждать это исключение(throws).

·        Написать метод, перехватывающий и обрабатывающий исключение (try-catch), возбуждаемое другим методом.

Исключение: с консоли вводится имя файла. Обработать ситуацию отсутствия файла (try с ресурсами).  Прочитать из файла строку символов и вывести её на консоль.

 

8.     Лабораторная работа №2. Создание и обработка собственных исключений.

В программе требуется:

·        Создать собственное исключение (class).

·        Создать метод (throw), который может возбуждать это исключение(throws).

·        Написать метод, перехватывающий и обрабатывающий исключение (try-catch), возбуждаемое другим методом.

Исключение: с консоли вводятся числа и записываются в массив. Не допускается запись в массив нулевых или отрицательных элементов.

 

9.     Лабораторная работа №2. Создание и обработка собственных исключений.

В программе требуется:

·        Создать собственное исключение (class).

·        Создать метод (throw), который может возбуждать это исключение(throws).

·        Написать метод, перехватывающий и обрабатывающий исключение (try-catch), возбуждаемое другим методом.

Исключение: с консоли вводится 10 чисел и записываются в массив. Добиться ввода только нечетных чисел.

 

10. Лабораторная работа №2. Создание и обработка собственных исключений.

В программе требуется:

·        Создать собственное исключение (class).

·        Создать метод (throw), который может возбуждать это исключение(throws).

·        Написать метод, перехватывающий и обрабатывающий исключение (try-catch), возбуждаемое другим методом.

Создать класс MDouble, который ведет себя как double, но не допускает значений POSITIVE_INFINITY и NaN. Получение этих значений рассматривать как исключительную ситуацию. Вместо значений POSITIVE_INFINITY записывать 99999. Вместо значений NaN записывать 0. Рассмотреть задачу: заданы два массива произвольных значений типа MDouble X и Y. Получить третий массив Z: zi = xi/yi;

 

11. Лабораторная работа №2. Создание и обработка собственных исключений.

В программе требуется:

·        Создать собственное исключение (class).

·        Создать метод (throw), который может возбуждать это исключение(throws).

·        Написать метод, перехватывающий и обрабатывающий исключение (try-catch), возбуждаемое другим методом.

Создать класс MDouble, который ведет себя как double, но не допускает значений NaN. Получение этих значений рассматривать как исключительную ситуацию. Вместо значений NaN записывать 77777. Рассмотреть задачу: заданы два массива произвольных значений типа MDouble A и B. Получить третий массив C: c i= ai%bi;

 

12.  Лабораторная работа №2. Создание и обработка собственных исключений.

      В программе требуется:

·        Создать собственное исключение (class).

·        Создать метод (throw), который может возбуждать это исключение(throws).

·        Написать метод, перехватывающий и обрабатывающий исключение (try-catch), возбуждаемое другим методом.

Создать класс MDouble, который ведет себя как double, но не допускает значений NEGATIVE_INFINITY и NaN. Получение этих значений рассматривать как исключительную ситуацию. Вместо значений NEGATIVE_INFINITY записывать -99999. Вместо значений NaN записывать 0. Рассмотреть задачу: заданы два массива произвольных значений типа MDouble K и M. Получить третий массив L: li = ki/mi;

 

13.  Лабораторная работа №2. Создание и обработка собственных исключений.

 В программе требуется:

·        Создать собственное исключение (class).

·        Создать метод (throw), который может возбуждать это исключение(throws).

·        Написать метод, перехватывающий и обрабатывающий исключение (try-catch), возбуждаемое другим методом.

Создать класс MInt, который ведет себя как int, но допускает только положительные значения от 0 до 255. Получение других значений рассматривать как исключительную ситуацию. Класс Mint допускает следующие операции: 0/0=0, 1/0=1. Рассмотреть задачу: заданы два массива произвольных значений типа MInt K и M. Получить третий массив L: li = ki/mi;

 

14.  Лабораторная работа №2. Создание и обработка собственных исключений.

 В программе требуется:

·        Создать собственное исключение (class).

·        Создать метод (throw), который может возбуждать это исключение(throws).

·        Написать метод, перехватывающий и обрабатывающий исключение (try-catch), возбуждаемое другим методом.

Исключение: с консоли вводятся 15 чисел и записываются в массив. Добиться ввода только чисел, делящихся без остатка на 3 и 8 одновременно.

 

15.  Лабораторная работа №2. Создание и обработка собственных исключений.

 В программе требуется:

·        Создать собственное исключение (class).

·        Создать метод (throw), который может возбуждать это исключение(throws).

·        Написать метод, перехватывающий и обрабатывающий исключение (try-catch), возбуждаемое другим методом.

Создать класс, который ведет себя как float, но не допускающий значений >+35767 и < -35768, а также значений POSITIVE_INFINITY, NEGATIVE_INFINITY, NaN вместо них записывать 33333. Выход за пределы значений >+35767 и < -35768, а также значение POSITIVE_INFINITY рассматривать как исключительную ситуацию. Использовать класс для выполнения арифметических операций (x/y x%y).

 

16.  Лабораторная работа №2. Создание и обработка собственных исключений.

В программе требуется:

·        Создать собственное исключение (class).

·        Создать метод (throw), который может возбуждать это исключение(throws).

·        Написать метод, перехватывающий и обрабатывающий исключение (try-catch), возбуждаемое другим методом.

Исключение: с консоли вводится число, проверять может ли это число быть ценой товара (LL.00 - LL.99). Добиться ввода только правильных цен.

 

17. Лабораторная работа №2. Создание и обработка собственных исключений.

В программе требуется:

·        Создать собственное исключение (class).

·        Создать метод (throw), который может возбуждать это исключение(throws).

·        Написать метод, перехватывающий и обрабатывающий исключение (try-catch), возбуждаемое другим методом.

Исключение: с консоли имена членов клуба. Добиться ввода только кличек из заранее подготовленного списка.

 

18. Лабораторная работа №2. Создание и обработка собственных исключений.

В программе требуется:

·        Создать собственное исключение (class).

·        Создать метод (throw), который может возбуждать это исключение(throws).

·        Написать метод, перехватывающий и обрабатывающий исключение (try-catch), возбуждаемое другим методом.

Исключение: создать класс MyString, который не допускает строк длиннее семи символов. Сформировать массив объектов типа MyString.